A* is a pathing finding algorithm that allows the user to input a starting node and ending node and it will find a path between the 2.

The board is 15x15 and each node has a 10% chance of being unpathable, the path cannot go through unpathable nodes. 

